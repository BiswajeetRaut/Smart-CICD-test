build: echo "Building utils library"
tests:
  unit: echo "No tests yet for utils"
deploy:
  staging: echo "Publishing utils to staging registry..."
  production: echo "Publishing utils to production registry..."
